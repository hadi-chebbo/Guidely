<div class="card">
    <h3>{{ $major['name'] }}</h3>

    <span class="badge">{{ $major['category'] }}</span>
    <span class="badge">{{ $major['demand'] }} Demand</span>

    <p>{{ $major['description'] }}</p>

    <a href="#">View details</a>
</div>